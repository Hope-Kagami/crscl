import 'package:flutter/material.dart';

class BookingFailureScreen extends StatelessWidget {
  const BookingFailureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Booking Failed')),
      body: Center(child: Text('Booking Failure Screen Stub')),
    );
  }
}
