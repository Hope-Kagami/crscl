import 'package:flutter/material.dart';

class ServiceCenterDetailsScreen extends StatelessWidget {
  const ServiceCenterDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Service Center Details')),
      body: Center(child: Text('Service Center Details Screen Stub')),
    );
  }
}
